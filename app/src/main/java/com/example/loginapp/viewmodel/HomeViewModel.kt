package com.example.loginapp.viewmodel

import androidx.lifecycle.ViewModel
import com.example.loginapp.data.LoginAppRepository

class HomeViewModel(private val repository: LoginAppRepository): ViewModel() {
}